# Git hooks for at-include's own repo.
#
# This file does double duty: it's what keeps *this* repo's commits clean,
# and it's the worked example the README points readers at when it suggests
# wiring at-include's own `--check` into a pre-push (or pre-commit) hook in
# their own project via lefthook.
#
# Install locally with:
#   mise exec -- lefthook install
# (or just `lefthook install` if lefthook is already on PATH/shimmed).
# `lefthook install` writes into .git/hooks/, which is untracked local state —
# every clone of this repo needs to run it once after checkout.
pre-commit:
  # Jobs run in the order listed below (parallel is NOT set at this level,
  # so it defaults to sequential — see docs/configuration/parallel.md).
  # This matters: gofumpt below mutates .go files on disk (and re-stages the
  # fixed versions), so it must finish completely *before* anything that
  # reads those files — golangci-lint and go test — starts. Running gofumpt
  # concurrently with lint/test via a blanket `parallel: true` would be a
  # genuine data race: lint or test could read a file mid-rewrite.
  #
  # Once formatting is settled, lint and test don't touch each other's inputs
  # (both are read-only against the same on-disk state), so they're grouped
  # together and parallelized to keep the hook fast.
  jobs:
    - name: gofumpt
      glob: "*.go"
      run: gofumpt -l -w {staged_files}
      stage_fixed: true

    # Dogfooding: regenerate this repo's own AGENTS.md whenever AGENTS.src.md
    # (or one of its imports, currently docs/architecture.md) changes.
    # `stage_fixed: true` stages the regenerated AGENTS.md so it joins the
    # same commit instead of failing the commit over a stale file.
    #
    # docs/conventions.md is deliberately NOT in this glob: it's meant to be
    # read directly by humans and agents alike, not consumed only through the
    # generated AGENTS.md, so it isn't an @import target at all. See
    # AGENTS.src.md for what it imports instead.
    #
    # This runs after gofumpt and before the lint/test group, on its own line
    # (not folded into the parallel group below) for the same reason gofumpt
    # itself isn't: it writes to a file on disk (AGENTS.md) that a concurrent
    # job could conceivably read (e.g. a future job that greps docs), so it
    # stays part of the sequential, mutation-only phase rather than being
    # added to the read-only parallel group — avoiding the same
    # gofumpt-mutation race this file already calls out above.
    #
    # Uses `go run ./cmd/at-include` rather than `dist/at-include`: dist/ is
    # gitignored and only exists after someone has run `mise run build`, so a
    # fresh clone that runs `lefthook install` and immediately commits would
    # have no dist/at-include yet. `go run` builds-and-runs from source on
    # demand, so the hook works right after clone with no separate build
    # step, at the cost of a fraction of a second of compile time per commit.
    #
    # This job used to carry `exclude: ["examples/**"]`, because
    # examples/consumer-smoke-test/ had its own AGENTS.src.md and the exclude
    # was believed necessary to stop an edit there from regenerating the *root*
    # AGENTS.md. That directory is gone (replaced by the post-release smoke
    # test in .github/workflows/release-smoke-test.yml), and the exclude went
    # with it — verified against lefthook v2.1.10 rather than assumed: staging
    # only test/cases/single-import/files/AGENTS.src.md with no exclude in
    # place reports "agents-build (skip) no matching staged files", while
    # staging the root AGENTS.src.md runs the job. So this glob matches on the
    # repo-relative path, not the basename, and the 21 AGENTS.src.md fixtures
    # under test/cases/** do not trigger it.
    #
    # Note that anchoring the glob as "./AGENTS.src.md" does NOT work — it
    # silently matches nothing, disabling the job.
    - name: agents-build
      glob: "AGENTS.src.md"
      run: go run ./cmd/at-include
      stage_fixed: true

    - group:
        parallel: true
        jobs:
          - name: lint
            glob: "*.go"
            # Runs across the whole module rather than just staged files.
            # golangci-lint's own config-aware caching (see .golangci.yml)
            # keeps repeat runs fast, and whole-module analysis is what
            # catches issues a staged-files-only run would miss (e.g. an
            # unused export that's only unused because of an edit in a
            # different, unstaged file). Worth the extra second or two on a
            # module this size.
            run: golangci-lint run

          - name: test
            glob: "*.go"
            run: go test ./... -count=1

# pre-push runs the same checks CI runs (see mise.toml's `check` task), as a
# last local gate before code leaves the machine. It's on pre-push rather
# than pre-commit because `mise run check` runs the full test suite plus
# lint plus a formatting check every time — worth doing once before a push,
# too slow to want on every single commit.
pre-push:
  jobs:
    - name: check
      run: mise run check

    # Blocks pushes to master that add commits without a Changie fragment
    # under .changes/unreleased/. lefthook does NOT forward the pre-push
    # stdin (lines of `<local-ref> <local-sha> <remote-ref> <remote-sha>`,
    # one per ref being pushed) to a job's `run:` body by default — that was
    # verified directly: a naive `run: | while read ...` job read zero
    # lines, both via `lefthook run pre-push` and a real `git push` through
    # an installed hook. `use_stdin: true` is required to receive it.
    #
    # lefthook executes `run:` blocks via `sh -c`, not bash (verified by
    # probing the actual command run for a job's PID) — so this block must
    # stay POSIX/dash-safe: single `[ ]`, not bash's `[[ ]]`. On macOS
    # /bin/sh happens to be bash in POSIX mode so `[[ ]]` works there by
    # luck, but on Debian/Ubuntu (most Linux CI runners and dev machines)
    # /bin/sh is dash, where `[[` is a syntax error and the check would be
    # silently skipped.
    #
    # `status` is accumulated across the whole loop and exited explicitly
    # at the end, rather than letting the loop's exit code fall through:
    # with multiple refs pushed in one command (e.g.
    # `git push origin master feature-x`), the shell loop's exit status is
    # otherwise just whatever the LAST line produced — so a failing master
    # check followed by a passing (or non-matching) line would silently
    # overwrite the failure and let the push through.
    - name: check-changelog-fragment
      use_stdin: true
      run: |
        status=0
        while read -r local_ref local_sha remote_ref remote_sha; do
          if [ "$remote_ref" = "refs/heads/master" ]; then
            ./.github/scripts/check-changelog-fragment.sh . "$remote_sha" "$local_sha" || status=1
          fi
        done
        exit "$status"
