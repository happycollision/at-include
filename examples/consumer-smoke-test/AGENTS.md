> [!IMPORTANT]
> **This file is generated. Do not edit it directly — edits here will be
> overwritten.**
>
> Edit [`AGENTS.src.md`](AGENTS.src.md) instead, then regenerate:
> `at-include --src AGENTS.src.md --out AGENTS.md`
>
> **If you are an agent** and were asked to change `AGENTS.md`, make the change
> in `AGENTS.src.md` and regenerate — do not edit this file.

# Consumer smoke test

This directory installs `at-include` from its published release via `mise.toml`,
exactly the way the README tells a consumer to. It exists to catch installation
regressions — that the release archives resolve, the binary runs, and the
`--check` CI gate behaves.

Contents of notes/setup.md (project instructions, checked into the codebase):

## Setup

Run `mise install`.

Contents of notes/shared.md (project instructions, checked into the codebase):

Shared note, imported from a nested file.



Things that must stay literal: `@notes/setup.md` in a code span,
an email like dev@example.com, and a package such as @scope/pkg.

```
@notes/setup.md
```
